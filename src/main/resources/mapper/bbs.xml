<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multi.werin.bbs.BbsMapper">

    <select id="getBbsList" resultType="com.multi.werin.bbs.BbsVO">
        SELECT * FROM bbs;
    </select>

    <select id="getBbsById" parameterType="int" resultType="com.multi.werin.bbs.BbsVO">
        SELECT * FROM bbs WHERE bbs_id = #{bbsId};
    </select>

    <insert id="createBbs" parameterType="com.multi.werin.bbs.BbsVO">
        INSERT INTO bbs (cate, title, content, writer, created_at, bbs_like, bbs_view, bbs_img)
        VALUES (#{cate}, #{title}, #{content}, #{writer}, #{created_at}, #{bbs_like}, #{bbs_view}, #{bbs_img});
    </insert>

    <update id="updateBbs" parameterType="com.multi.werin.bbs.BbsVO">
        UPDATE bbs SET cate = #{cate}, title = #{title}, content = #{content},
        writer = #{writer}, created_at = #{created_at}, bbs_like = #{bbs_like}, bbs_view = #{bbs_view}, bbs_img = #{bbs_img}
        WHERE bbs_id = #{bbs_id};
    </update>

    <delete id="deleteBbs" parameterType="int">
        DELETE FROM bbs WHERE bbs_id = #{bbsId};
    </delete>

</mapper>