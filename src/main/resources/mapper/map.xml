<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="map">
    <insert id="insert" parameterType="MapVO">
        <!-- Your SQL query for inserting into map table -->
        INSERT INTO map (map_location_x, map_location_y, map_address, map_address_detail, map_image)
        VALUES (#{mapLocationX}, #{mapLocationY}, #{mapAddress}, #{mapAddressDetail}, #{mapImage})
    </insert>
</mapper>